Problems A-D2 are relatively simple.

Problem E
给定10^6以内的整数w和b，要求在一个10^9x10^9棋盘上找出一块连通区域（两个单元格有公共边即为connected），其中包含w个白块及b个黑块。
该题是构造题，需要我们构造出一个连通区域满足条件。
不妨假设b>w；
我们选择一条水平的由单元格组成的线，称为‘central line’，其中两端都是白色，并且其中包含恰好w个白块；
那么central line里必然包含(w-1)个黑块；
此时，我们还剩下(b-(w-1))个黑块未被包含在该连通区域中；
我们可以看到，在central line的左右两边各有一块黑色单元格，我们可以将它们加入连通区域；
如果还有黑块未被包含在区域，我们还可以考虑在central line上下两侧紧挨着它分布的2w个黑块；
不断地加入这些黑块，直到条件被满足。
如果在上述的黑块都被加入之后，条件仍然未被满足，则我们无法构造出满足条件的连通区域（此时有b>3w+1）。
